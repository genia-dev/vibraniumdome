apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.opensearchDashboards.name }}
spec:
  type: ClusterIP
  ports:
  - port: 5601
    targetPort: 5601
    protocol: TCP
    name: http
  selector:
    app: {{ .Values.opensearchDashboards.name }}
