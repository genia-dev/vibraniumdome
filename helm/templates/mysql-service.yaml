apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.db.name }}
spec:
  type: ClusterIP
  ports:
  - port: {{ (index .Values.db.ports 0).containerPort }}
    targetPort: {{ (index .Values.db.ports 0).containerPort }}
  selector:
    app: {{ .Values.db.name }}
